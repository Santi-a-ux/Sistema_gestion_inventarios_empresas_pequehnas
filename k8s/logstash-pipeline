apiVersion: v1
kind: ConfigMap
metadata:
  name: logstash-pipeline
  namespace: product-system
data:
  logstash.conf: |
    input {
      tcp {
        port => 5000
        codec => json
      }
    }
    output {
      stdout { codec => rubydebug }
    }